export const night = () => {
  // resolve state after night finishes
  // example: show all people who died by which action
};

export const day = () => {
  // resolve state after day finishes
  // example: show all people that revealed their role
};

export const evening = () => {
  // resolve state after evening finishes
  // example: show all people that where lynched
};
